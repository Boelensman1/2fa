# pnpm reads .npmrc for registry and auth only; its own settings live in
# pnpm-workspace.yaml. What is left here is for npm, which flake.nix's preBuild
# uses for `npm rebuild keytar bufferutil` and which would otherwise pick up
# ignore-scripts=true from a global ~/.npmrc.
ignore-scripts=false
